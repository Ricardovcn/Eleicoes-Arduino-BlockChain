- Para instalar Web3

    sudo apt-get update
    sudo apt-get install python3-dev
    sudo apt-get install libevent-dev
    pip3 install --upgrade web3

- Para instalar ganache-cli

    sudo apt-get install build-essential libssl-dev
    curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.11/install.sh | bash

    # Instalar a última versão estável do Node.js
    nvm install 10.14.1

    npm i -g ganache-cli

- Para instalar solidity compiler.

    # Não é necessério instalar por enquanto, pois o ABI e ADDRESS do contrato eleição
    # foram inseridos diretamente no 'deploy_contract.py'. Não foi possível compilar
    # o arquivo '.sol' a partir do solidity compiler.

    pip install py-solc
    python -m solc.install v0.4.25


    sudo add-apt-repository ppa:ethereum/ethereum
    sudo apt-get update
    sudo apt-get install solc

